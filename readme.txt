Readme

Student Database

main.cpp is the driver class which contains the event loop

studentDB.cpp contains the defintions of the functions used to access and manipulate the data

student.h is the header file which declares the functions in studentDB.cpp

the Student Database file is where the program reads and writes to.

to run the program navigate to the folder in the terminal and type "make run" to compile and run
